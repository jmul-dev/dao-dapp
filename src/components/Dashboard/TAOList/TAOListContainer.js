import { connect } from "react-redux";
import { TAOList } from "./TAOList";

const mapStateToProps = (state) => {
	return {};
};

const mapDispatchToProps = (dispatch) => {
	return {};
};

export const TAOListContainer = connect(
	mapStateToProps,
	mapDispatchToProps
)(TAOList);
